(ns
  ^{:author "Frank V. Castellucci"
      :doc "spike space"}
  clasew.sandbox
  (:require [clojure.pprint :refer :all]
            [clojure.set :as set])
  )

;; Demonstrate record coercion
(def p pprint)

